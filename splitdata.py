#!/Users/takashi/.pyenv/shims/python


def makedataset(ratio1, ratio2, ratio3, data):
    """
        Split the dataset to train, validate,
        and test the calculation.
        ratio1: ratio for training dataset
        ratio2: ratio for validating dataset
        ratio3: ratio for testing dataset
        data: Numpy array(Matrix)
    """


def test_makedataset():
    
